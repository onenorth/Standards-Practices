module.exports = function(grunt) {

    /**
     * This task is for demonstration purposes only. Here, we define a custom task
     * that will execute whenever we run:
     *
     * $ grunt custom
     */
    grunt.registerTask('custom', function() {

        // Wondrous events occur here.

    });

};
